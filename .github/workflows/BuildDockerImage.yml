name: Build a Docker image and Push it to ACR

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - uses: azure/docker-login@v1
        with:
          login-server: workflowreg.azurecr.io,
          username: 'workflowreg'
          password: 'A3J1ct+Dxaenen91ADXVlXXIjMr1Nfht'

      - run: |
          docker build . -t workflowreg.azurecr.io/appdb:${{ github.sha }}
          docker push workflowreg.azurecr.io/appdb:${{ github.sha }}
